describe(`If you see this and still decide to use this module I don't know what to tell you except ... good luck.`, () => {
  it('does things', () => {
    // Probably?
    expect(true).toBe(true);
  });
});
